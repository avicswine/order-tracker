{
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix customer-portal && npm run build --prefix customer-portal && npm install --prefix backend && cd backend && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/server.js"
  }
}
